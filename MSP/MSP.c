# include <stdlib.h>
# include <stdio.h>
# include "MSP.h"

/* input data */
extern unsigned num;
extern int data[];

int main() {
	printf("%d\n", maxSubarray(0, num));
}

int maxSubarray(unsigned from, unsigned num) {
	/* stop recursive call */
	/* divide */
	/* conquer */
	/* combine */
}
